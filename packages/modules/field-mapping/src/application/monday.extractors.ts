// packages/modules/monday-integration/src/application/monday.extractors.ts
// Smoke-test stub: required only to satisfy module resolution so the API can boot.
// If this function is actually called, it will throw (safe for boot-only smoke test).

export function extractRawFromColumnValue(_columnValue: unknown): unknown {
    throw new Error(
      "extractRawFromColumnValue is not implemented (smoke-test stub)."
    );
  }
  